services:
  app:
    container_name: app
    build: .
    develop:
      watch:
        - action: sync
          path: .
          target: ./app
          ignore:
            - node_modules/
    ports:
      - "5173:5173"
    stdin_open: true
    tty: true
